import { Brand } from '@/components/layout/Brand';
import { UdevBadge } from '@/components/status/UdevBadge';
import { notifyUnderDevelopment } from '@/app/notifications';
import type { Page } from '@/app/types';
export function LandingPage({setPage}:{setPage:(page:Page)=>void}){return <div className="landing"><header><Brand/><nav>{["Product","Capabilities","How it works","Resources"].map(n=><button key={n} onClick={()=>notifyUnderDevelopment(n)}>{n} <UdevBadge/></button>)}</nav><button className="outline" onClick={()=>setPage("inputs")}>Open workspace</button></header><main><section className="hero"><div><span className="eyebrow">Executive-led · Deterministic · Traceable</span><h1>Plan AI infrastructure from business requirements to procurement-ready outcomes.</h1><p>Capture global planning choices and workload-specific demand, then evaluate model, platform, capacity, resilience, BOM and financial outcomes through governed calculation stages.</p><div className="hero-actions"><button className="primary" onClick={()=>setPage("inputs")}>Start sizing</button><button onClick={()=>setPage("bom")}>View BOM layout</button></div><div className="proof"><span><b>Multiple</b> workloads</span><span><b>4</b> decision options</span><span><b>100%</b> traceable</span></div></div><div className="hero-card"><h2>Executive input baseline</h2>{["Global deployment and resilience","Total users and growth","Workload-specific demand units","Simple / Medium / Complex mix","Development and Production selection"].map(x=><p key={x}>✓ {x}</p>)}<div className="status">Approved input design</div></div></section><section className="flow"><h2>One controlled workflow</h2><div>{[["1","Business inputs"],["2","Model checkpoint"],["3","Infrastructure options"],["4","BOM and TCO"]].map(([n,t])=><article key={n}><b>{n}</b><h3>{t}</h3><p>{n==="1"?"Global and workload requirements":n==="2"?"Performance and price objectives":n==="3"?"Development and Production": "Evidence-backed commercial output"}</p>{n!=="1"&&<UdevBadge/>}</article>)}</div></section></main><footer><Brand/><span>Planning outputs require governed evidence, benchmarks and prices.</span></footer></div>}
